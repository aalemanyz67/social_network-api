//This will import the USER and THOUGHT models from files.
const User = require('./User');
const Thought = require('./Thought');

//this will export user and thought models 
module.exports = {Thought, User};
